Tinytroupe to artificialsocieties board briefing
Document · DOCX
ok, now make a technical overview for the it coder team with tasks and project overview for what should be implemented (leave out timelines, just state the implementation aspects to upgrade the tinytroupe code and make it run the backend for the features identified in artificial societies

13:03

Creating a comprehensive technical implementation guide for the development team to upgrade TinyTroupe for Artificial Societies features

Creating a comprehensive technical implementation guide for the development team to upgrade TinyTroupe for Artificial Societies features
text

================================================================================
TECHNICAL IMPLEMENTATION GUIDE
Upgrading TinyTroupe for Artificial Societies Feature Parity
================================================================================

PROJECT OVERVIEW

This document provides a comprehensive technical specification for extending
Microsoft's TinyTroupe framework to achieve feature parity with Artificial
Societies' simulation platform. The implementation focuses on adding social
network modeling, prediction accuracy, content generation, and production-ready
API infrastructure.

TARGET CAPABILITIES:
• Simulate 20-300 persona societies with social network effects
• Achieve 80-85% prediction accuracy for content engagement
• Generate 100+ content variants per simulation
• Integrate with LinkedIn and other social platforms
• Provide real-time simulation execution via REST API
• Support concurrent multi-user simulations at scale

================================================================================
SECTION 1: CORE ARCHITECTURE ENHANCEMENTS
================================================================================

1.1 ENHANCED TINYPERSON (PERSONA SYSTEM)
────────────────────────────────────────────────────────────────────────────

CURRENT STATE:
TinyPerson has basic properties (age, occupation, personality, interests) and
actions (listen, think, act) but lacks:
- Social network awareness (connections, influence)
- Engagement prediction capabilities
- Content affinity modeling
- Historical behavior tracking
- Probabilistic decision-making

IMPLEMENTATION TASKS:

[TASK 1.1.1] Extend TinyPerson Base Class
──────────────────────────────────────────
File: tinytroupe/agent.py (extend TinyPerson class)

Add new properties:
- social_connections: Dict[str, ConnectionEdge]
  - connection_id, strength (0.0-1.0), influence_score, interaction_history
- engagement_patterns: Dict[str, float]
  - content_type preferences (article, video, poll, etc.)
  - topic affinities (technology, business, health, etc.)
  - posting time preferences
  - engagement likelihood by content type
- behavioral_history: List[BehavioralEvent]
  - past interactions, reactions, content created
  - timestamp, action_type, content_id, outcome
- influence_metrics: InfluenceProfile
  - reach, authority, expertise_domains
  - follower_to_following_ratio, engagement_rate
- prediction_confidence: float (0.0-1.0)
  - reliability score for this persona's predictions

Add new methods:
- calculate_engagement_probability(content: Content) -> float
  - Analyze content features (topic, format, length, tone)
  - Apply persona's engagement patterns
  - Factor in social influence from connections
  - Return probability of engagement (like, comment, share)

- predict_reaction(content: Content) -> Reaction
  - Determine reaction type (like, love, insightful, celebrate)
  - Generate comment if applicable (using LLM)
  - Decide on sharing behavior
  - Calculate virality coefficient

- update_from_interaction(interaction: Interaction) -> None
  - Learn from actual interactions (for accuracy improvement)
  - Update engagement_patterns based on real behavior
  - Adjust influence_metrics
  - Improve prediction_confidence over time

- get_content_affinity(content: Content) -> float
  - Score content relevance to persona
  - Consider topic alignment, format preference
  - Factor in current trends in persona's network

Implementation notes:
- Use dataclasses for structured data (ConnectionEdge, BehavioralEvent, etc.)
- Implement caching for expensive calculations
- Add JSON serialization for persistence


[TASK 1.1.2] Behavioral Trait System
─────────────────────────────────────
File: tinytroupe/agent_traits.py (new module)

Create granular personality traits that affect engagement:
- OpennessToNewIdeas: 0.0-1.0 (affects likelihood to engage with novel content)
- Conscientiousness: 0.0-1.0 (affects posting regularity, thoughtfulness)
- Extraversion: 0.0-1.0 (affects sharing frequency, network activity)
- Agreeableness: 0.0-1.0 (affects commenting positivity, conflict avoidance)
- ControversialityTolerance: 0.0-1.0 (affects engagement with divisive topics)
- InformationSeekingBehavior: 0.0-1.0 (long-form vs short-form preference)
- VisualContentPreference: 0.0-1.0 (image/video vs text preference)

Implement TraitBasedBehaviorModel:
- compute_action_probability(persona: TinyPerson, action: Action) -> float
- apply_trait_modifiers(base_probability: float, traits: Dict) -> float
- generate_trait_profile_from_description(description: str) -> TraitProfile

Use LLM to infer traits from persona descriptions:
- Prompt engineering to extract traits from text
- Validate trait consistency
- Generate synthetic trait variations


[TASK 1.1.3] Memory and Context System
───────────────────────────────────────
File: tinytroupe/memory.py (extend)

Current memory is basic. Enhance with:

SHORT-TERM MEMORY:
- Last N interactions in simulation
- Current conversation context
- Recent content seen
- Immediate network activity

LONG-TERM MEMORY:
- Persona background/history
- Deep-seated preferences
- Past simulation outcomes
- Learned behaviors

SEMANTIC MEMORY:
- Knowledge base about topics
- Expertise domains
- Opinion formation history

Implementation:
- Use vector embeddings for semantic search
- Implement memory decay (older memories less influential)
- Add memory consolidation (frequent patterns → long-term memory)
- Integrate with ChromaDB or Pinecone for vector storage

Methods to implement:
- store_interaction(interaction: Interaction) -> None
- retrieve_relevant_memories(query: str, k: int) -> List[Memory]
- consolidate_memories() -> None
- get_memory_summary() -> str


[TASK 1.1.4] Persona Factory Enhancement
─────────────────────────────────────────
File: tinytroupe/factory.py (extend TinyPersonFactory)

Enhance persona generation to create diverse, realistic personas:

Add generation strategies:
- generate_from_demographics(age_range, location, occupation, interests) -> TinyPerson
- generate_from_linkedin_profile(profile_data: Dict) -> TinyPerson
- generate_persona_cluster(archetype: str, count: int) -> List[TinyPerson]
  - archetypes: "early_adopter", "skeptic", "influencer", "lurker", etc.
- generate_diverse_population(size: int, distribution: Dict) -> List[TinyPerson]

Implement persona validation:
- ensure_consistency(persona: TinyPerson) -> bool
  - Check trait coherence
  - Validate demographic consistency
  - Ensure behavioral patterns align with archetype
- calculate_diversity_score(personas: List[TinyPerson]) -> float
  - Measure demographic diversity
  - Measure behavioral diversity
  - Ensure sufficient variation in population

Add persona templates library:
- Load pre-defined persona archetypes
- Support custom template creation
- Template inheritance and composition


================================================================================
SECTION 2: SOCIAL NETWORK ENGINE
================================================================================

2.1 GRAPH-BASED NETWORK MODELING
────────────────────────────────────────────────────────────────────────────

OBJECTIVE:
Model social networks with realistic topology, influence propagation, and
community structures. This is CRITICAL for Artificial Societies parity.

[TASK 2.1.1] Network Data Model
────────────────────────────────
File: tinytroupe/social_network.py (new module)

Define core network structures:

class Connection:
    """Represents a connection between two personas"""
    source_id: str
    target_id: str
    strength: float  # 0.0-1.0
    relationship_type: str  # "follower", "friend", "colleague", "family"
    interaction_frequency: float  # interactions per week
    last_interaction: datetime
    influence_score: float  # how much target influences source
    created_at: datetime

class NetworkTopology:
    """Represents the entire social network structure"""
    nodes: Dict[str, TinyPerson]  # persona_id -> persona
    edges: List[Connection]
    adjacency_matrix: np.ndarray  # for fast lookups
    influence_matrix: np.ndarray  # directed influence weights
    communities: List[Community]  # detected communities

    Methods:
    - add_persona(persona: TinyPerson) -> None
    - add_connection(source_id: str, target_id: str, **kwargs) -> Connection
    - remove_connection(connection_id: str) -> None
    - get_neighbors(persona_id: str, depth: int = 1) -> List[TinyPerson]
    - get_influence_path(source_id: str, target_id: str) -> List[str]
    - calculate_centrality_metrics() -> Dict[str, float]
    - detect_communities() -> List[Community]

class Community:
    """Represents a cluster of closely connected personas"""
    community_id: str
    members: List[str]  # persona_ids
    density: float
    central_personas: List[str]  # most influential in community
    shared_interests: List[str]
    avg_engagement_rate: float


[TASK 2.1.2] Network Generation Algorithms
───────────────────────────────────────────
File: tinytroupe/network_generator.py (new module)

Implement realistic network topologies:

SCALE-FREE NETWORK (Barabási-Albert model):
- Preferential attachment: new nodes connect to high-degree nodes
- Power-law degree distribution (realistic for social networks)
- Implementation: generate_scale_free_network(n: int, m: int) -> NetworkTopology

SMALL-WORLD NETWORK (Watts-Strogatz model):
- High clustering coefficient + short path lengths
- Realistic for real-world social networks
- Implementation: generate_small_world_network(n: int, k: int, p: float) -> NetworkTopology

COMMUNITY-BASED NETWORK:
- Generate communities first, then connect them
- Within-community density > between-community density
- Implementation: generate_community_network(num_communities: int,
                                            community_sizes: List[int]) -> NetworkTopology

LINKEDIN-STYLE NETWORK:
- Professional connections based on industry, company, role
- Endorsements and recommendations as weighted edges
- Implementation: generate_professional_network(personas: List[TinyPerson]) -> NetworkTopology

For each generation method, include parameters for:
- Network density
- Clustering coefficient
- Average path length
- Degree distribution
- Homophily (tendency to connect with similar personas)


[TASK 2.1.3] Influence Propagation System
──────────────────────────────────────────
File: tinytroupe/influence.py (new module)

Model how content and behaviors spread through the network:

INDEPENDENT CASCADE MODEL:
- Each activated node independently activates neighbors with probability
- Probabilistic spread similar to viral content
- Implementation: simulate_cascade(network: NetworkTopology,
                                    seed_nodes: List[str],
                                    content: Content) -> CascadeResult

LINEAR THRESHOLD MODEL:
- Node activates when sum of activated neighbor weights exceeds threshold
- Models peer pressure and social proof
- Implementation: simulate_threshold_propagation(network: NetworkTopology,
                                                  seed_nodes: List[str],
                                                  thresholds: Dict[str, float]) -> PropagationResult

INFORMATION DIFFUSION:
- Track how information spreads with decay over time and distance
- Model information freshness, relevance decay
- Implementation: simulate_information_spread(network: NetworkTopology,
                                               initial_content: Content,
                                               time_steps: int) -> DiffusionResult

Key components to implement:

class InfluencePropagator:
    def __init__(self, network: NetworkTopology, model: str = "cascade"):
        self.network = network
        self.model = model

    def propagate(self, seed_personas: List[str], content: Content) -> PropagationResult:
        """Main propagation simulation"""
        activated = set(seed_personas)
        activation_times = {pid: 0 for pid in seed_personas}

        for time_step in range(self.max_steps):
            newly_activated = self._propagate_step(activated, content, time_step)
            activated.update(newly_activated)

            if not newly_activated:  # convergence
                break

        return PropagationResult(
            activated_personas=activated,
            activation_times=activation_times,
            total_reach=len(activated),
            cascade_depth=max(activation_times.values()),
            engagement_by_time=self._compute_engagement_curve()
        )

    def _propagate_step(self, activated: Set[str], content: Content, time: int) -> Set[str]:
        """Single step of propagation"""
        # Implement based on selected model
        pass

    def calculate_influence_score(self, persona_id: str) -> float:
        """Calculate overall influence of a persona"""
        # Combine: centrality, past engagement rates, follower quality
        pass


[TASK 2.1.4] Network Analysis Tools
────────────────────────────────────
File: tinytroupe/network_analysis.py (new module)

Provide analytics for understanding network dynamics:

CENTRALITY METRICS:
- degree_centrality(network: NetworkTopology) -> Dict[str, float]
- betweenness_centrality(network: NetworkTopology) -> Dict[str, float]
- closeness_centrality(network: NetworkTopology) -> Dict[str, float]
- eigenvector_centrality(network: NetworkTopology) -> Dict[str, float]
- pagerank(network: NetworkTopology) -> Dict[str, float]

COMMUNITY DETECTION:
- louvain_communities(network: NetworkTopology) -> List[Community]
- label_propagation_communities(network: NetworkTopology) -> List[Community]
- girvan_newman_communities(network: NetworkTopology) -> List[Community]

NETWORK METRICS:
- calculate_density(network: NetworkTopology) -> float
- calculate_clustering_coefficient(network: NetworkTopology) -> float
- calculate_average_path_length(network: NetworkTopology) -> float
- calculate_assortativity(network: NetworkTopology, attribute: str) -> float
- identify_key_influencers(network: NetworkTopology, top_k: int) -> List[str]

VISUALIZATION SUPPORT:
- export_to_networkx(network: NetworkTopology) -> nx.Graph
- export_to_json(network: NetworkTopology) -> Dict
- generate_layout(network: NetworkTopology, algorithm: str) -> Dict[str, Tuple]


[TASK 2.1.5] Graph Database Integration
────────────────────────────────────────
File: tinytroupe/graph_store.py (new module)

Integrate with Neo4j for efficient graph queries:

class Neo4jNetworkStore:
    def __init__(self, uri: str, user: str, password: str):
        self.driver = GraphDatabase.driver(uri, auth=(user, password))

    def store_network(self, network: NetworkTopology) -> None:
        """Persist network to Neo4j"""
        with self.driver.session() as session:
            # Create persona nodes
            for persona in network.nodes.values():
                session.run(
                    "CREATE (p:Persona {id: $id, name: $name, ...}) ",
                    id=persona.id,
                    name=persona.name,
                    # ... other properties
                )

            # Create connection edges
            for connection in network.edges:
                session.run(
                    "MATCH (a:Persona {id: $source}), (b:Persona {id: $target}) "
                    "CREATE (a)-[r:CONNECTED {strength: $strength, ...}]->(b)",
                    source=connection.source_id,
                    target=connection.target_id,
                    strength=connection.strength
                )

    def query_neighbors(self, persona_id: str, depth: int = 1) -> List[Dict]:
        """Query network neighbors efficiently"""
        with self.driver.session() as session:
            result = session.run(
                f"MATCH (p:Persona {{id: $pid}})-[*1..{depth}]-(neighbor) "
                "RETURN neighbor",
                pid=persona_id
            )
            return [record["neighbor"] for record in result]

    def find_influence_path(self, source_id: str, target_id: str) -> List[str]:
        """Find shortest influence path between two personas"""
        with self.driver.session() as session:
            result = session.run(
                "MATCH path = shortestPath((a:Persona {id: $source})-[*]-(b:Persona {id: $target})) "
                "RETURN path",
                source=source_id,
                target=target_id
            )
            return self._extract_path(result.single()["path"])

    def get_community_members(self, community_id: str) -> List[str]:
        """Get all personas in a community"""
        pass

Cypher query templates to implement:
- Find influencers: personas with high PageRank
- Find bridges: personas connecting different communities
- Find similar personas: based on engagement patterns
- Recommend connections: based on shared interests/behaviors


================================================================================
SECTION 3: PREDICTION ENGINE
================================================================================

3.1 ENGAGEMENT PREDICTION SYSTEM
────────────────────────────────────────────────────────────────────────────

OBJECTIVE:
Predict how personas will engage with content with 80-85% accuracy. This is
the CORE differentiator for Artificial Societies.

[TASK 3.1.1] Feature Engineering
─────────────────────────────────
File: tinytroupe/features.py (new module)

Extract features from content and personas for prediction:

CONTENT FEATURES:
class ContentFeatureExtractor:
    def extract(self, content: Content) -> Dict[str, float]:
        """Extract all content features"""
        return {
            # Format features
            "word_count": self._count_words(content.text),
            "has_image": 1.0 if content.images else 0.0,
            "has_video": 1.0 if content.video_url else 0.0,
            "has_link": 1.0 if content.external_links else 0.0,
            "has_hashtags": 1.0 if content.hashtags else 0.0,
            "num_hashtags": len(content.hashtags),

            # Linguistic features
            "sentiment_score": self._analyze_sentiment(content.text),
            "readability_score": self._calculate_readability(content.text),
            "formality_score": self._measure_formality(content.text),
            "emotional_intensity": self._detect_emotion_intensity(content.text),

            # Topic features (multi-label)
            "topic_technology": self._detect_topic(content.text, "technology"),
            "topic_business": self._detect_topic(content.text, "business"),
            "topic_health": self._detect_topic(content.text, "health"),
            # ... more topics

            # Engagement signals
            "has_question": 1.0 if "?" in content.text else 0.0,
            "has_cta": self._detect_call_to_action(content.text),
            "controversy_score": self._measure_controversy(content.text),

            # Timing features
            "hour_of_day": content.timestamp.hour,
            "day_of_week": content.timestamp.weekday(),
            "is_weekend": 1.0 if content.timestamp.weekday() >= 5 else 0.0,
        }

    def _analyze_sentiment(self, text: str) -> float:
        """Use VADER or transformer-based sentiment analysis"""
        # Implementation with transformers
        pass

    def _calculate_readability(self, text: str) -> float:
        """Flesch-Kincaid or similar readability metric"""
        pass

    def _detect_topic(self, text: str, topic: str) -> float:
        """Use zero-shot classification or keyword matching"""
        pass

PERSONA FEATURES:
class PersonaFeatureExtractor:
    def extract(self, persona: TinyPerson) -> Dict[str, float]:
        """Extract persona features"""
        return {
            # Demographics
            "age": persona.age,
            "age_group": self._categorize_age(persona.age),

            # Network features
            "num_connections": len(persona.social_connections),
            "avg_connection_strength": self._avg_connection_strength(persona),
            "influence_score": persona.influence_metrics.authority,
            "centrality_score": self._calculate_centrality(persona),

            # Behavioral features
            "engagement_rate": persona.engagement_patterns.get("overall_rate", 0.0),
            "posting_frequency": self._calculate_posting_frequency(persona),
            "avg_comment_length": self._avg_comment_length(persona),

            # Preference features
            "prefers_visual": persona.engagement_patterns.get("visual_preference", 0.5),
            "prefers_longform": persona.engagement_patterns.get("longform_preference", 0.5),
            "controversy_tolerance": persona.behavioral_traits.get("controversy_tolerance", 0.5),

            # Personality traits
            "openness": persona.behavioral_traits.get("openness", 0.5),
            "extraversion": persona.behavioral_traits.get("extraversion", 0.5),
            # ... other traits
        }

INTERACTION FEATURES:
class InteractionFeatureExtractor:
    def extract(self, persona: TinyPerson, content: Content, network: NetworkTopology) -> Dict[str, float]:
        """Extract features from persona-content interaction context"""
        return {
            # Network context
            "num_friends_engaged": self._count_engaged_friends(persona, content, network),
            "influencer_engaged": self._check_influencer_engagement(persona, content, network),
            "community_relevance": self._calculate_community_relevance(persona, content, network),

            # Temporal context
            "time_since_last_engagement": self._time_since_last(persona),
            "recent_activity_level": self._recent_activity(persona),

            # Content-persona alignment
            "topic_alignment": self._calculate_topic_alignment(persona, content),
            "author_connection_strength": self._get_author_connection(persona, content),
        }


[TASK 3.1.2] Machine Learning Models
─────────────────────────────────────
File: tinytroupe/ml_models.py (new module)

Implement multiple prediction models:

BINARY ENGAGEMENT PREDICTION:
- Will persona engage (like/comment/share) with content? (yes/no)

class EngagementPredictor:
    """Predicts whether persona will engage with content"""

    def __init__(self):
        self.model = None  # Trained model
        self.feature_extractor = FeatureExtractor()
        self.scaler = StandardScaler()

    def train(self, training_data: List[TrainingExample]) -> None:
        """Train the prediction model"""
        # Extract features
        X = []
        y = []
        for example in training_data:
            features = self._extract_all_features(
                example.persona,
                example.content,
                example.network
            )
            X.append(features)
            y.append(1 if example.engaged else 0)

        X = np.array(X)
        y = np.array(y)

        # Scale features
        X_scaled = self.scaler.fit_transform(X)

        # Train model (try multiple algorithms)
        models = {
            "logistic": LogisticRegression(class_weight="balanced"),
            "random_forest": RandomForestClassifier(n_estimators=100, class_weight="balanced"),
            "xgboost": XGBClassifier(scale_pos_weight=self._calculate_pos_weight(y)),
            "lightgbm": LGBMClassifier(is_unbalance=True),
        }

        # Cross-validation to select best model
        best_score = 0
        best_model = None
        for name, model in models.items():
            scores = cross_val_score(model, X_scaled, y, cv=5, scoring="f1")
            avg_score = scores.mean()
            if avg_score > best_score:
                best_score = avg_score
                best_model = model

        # Train final model on all data
        self.model = best_model
        self.model.fit(X_scaled, y)

    def predict(self, persona: TinyPerson, content: Content, network: NetworkTopology) -> float:
        """Predict engagement probability"""
        features = self._extract_all_features(persona, content, network)
        features_scaled = self.scaler.transform([features])
        probability = self.model.predict_proba(features_scaled)[0][1]
        return probability

    def _extract_all_features(self, persona, content, network) -> np.ndarray:
        """Combine all feature extractors"""
        content_features = ContentFeatureExtractor().extract(content)
        persona_features = PersonaFeatureExtractor().extract(persona)
        interaction_features = InteractionFeatureExtractor().extract(persona, content, network)

        all_features = {**content_features, **persona_features, **interaction_features}
        return np.array(list(all_features.values()))

ENGAGEMENT TYPE PREDICTION:
- What type of engagement? (like, comment, share, no engagement)

class EngagementTypePredictor:
    """Predicts type of engagement (multi-class classification)"""

    def __init__(self):
        self.model = None
        self.label_encoder = LabelEncoder()

    def train(self, training_data: List[TrainingExample]) -> None:
        """Train multi-class classifier"""
        # Similar to binary predictor but with multi-class targets
        # Labels: "none", "like", "comment", "share", "like_and_comment", etc.
        pass

    def predict(self, persona: TinyPerson, content: Content) -> Dict[str, float]:
        """Predict probabilities for each engagement type"""
        pass

VIRALITY PREDICTION:
- Predict cascade size (how many will eventually engage)

class ViralityPredictor:
    """Predicts cascade size and virality coefficient"""

    def train(self, training_data: List[CascadeExample]) -> None:
        """Train regression model for cascade prediction"""
        pass

    def predict_cascade_size(self, content: Content, seed_personas: List[str],
                            network: NetworkTopology) -> int:
        """Predict total reach of content"""
        pass

ENSEMBLE MODEL:
- Combine multiple models for better accuracy

class EnsemblePredictor:
    """Combines multiple predictors for robust predictions"""

    def __init__(self):
        self.engagement_predictor = EngagementPredictor()
        self.type_predictor = EngagementTypePredictor()
        self.virality_predictor = ViralityPredictor()
        self.llm_predictor = LLMPredictor()  # Use LLM as another signal

    def predict(self, persona: TinyPerson, content: Content,
                network: NetworkTopology) -> PredictionResult:
        """Ensemble prediction"""
        # Get predictions from all models
        ml_prob = self.engagement_predictor.predict(persona, content, network)
        type_probs = self.type_predictor.predict(persona, content)
        llm_prediction = self.llm_predictor.predict(persona, content)

        # Weighted combination
        ensemble_prob = (
            0.5 * ml_prob +
            0.3 * type_probs.get("any_engagement", 0.0) +
            0.2 * llm_prediction.probability
        )

        return PredictionResult(
            engagement_probability=ensemble_prob,
            engagement_type_probs=type_probs,
            predicted_reaction=self._select_reaction(type_probs),
            confidence=self._calculate_confidence([ml_prob, type_probs, llm_prediction])
        )


[TASK 3.1.3] LLM-Based Prediction
──────────────────────────────────
File: tinytroupe/llm_predictor.py (new module)

Use LLMs as a prediction component (especially for qualitative aspects):

class LLMPredictor:
    """Use LLM reasoning for engagement prediction"""

    def __init__(self, model: str = "gpt-4"):
        self.model = model
        self.client = OpenAI()

    def predict(self, persona: TinyPerson, content: Content) -> LLMPrediction:
        """Use LLM to predict engagement"""

        prompt = self._construct_prediction_prompt(persona, content)

        response = self.client.chat.completions.create(
            model=self.model,
            messages=[
                {"role": "system", "content": PREDICTION_SYSTEM_PROMPT},
                {"role": "user", "content": prompt}
            ],
            temperature=0.3,
            response_format={"type": "json_object"}
        )

        prediction = json.loads(response.choices[0].message.content)

        return LLMPrediction(
            will_engage=prediction["will_engage"],
            probability=prediction["probability"],
            reasoning=prediction["reasoning"],
            predicted_reaction_type=prediction["reaction_type"],
            predicted_comment=prediction.get("comment", None)
        )

    def _construct_prediction_prompt(self, persona: TinyPerson, content: Content) -> str:
        """Build detailed prompt with persona and content context"""
        return f"""
You are predicting how a specific persona will react to content on a professional social network.

PERSONA PROFILE:
{persona.get_detailed_description()}

Key traits:
- Age: {persona.age}
- Occupation: {persona.occupation}
- Interests: {", ".join(persona.interests)}
- Personality: {persona.personality_traits}
- Engagement patterns: {persona.engagement_patterns}

CONTENT TO EVALUATE:
{content.text}

Content metadata:
- Format: {"has images" if content.images else "text only"}
- Topics: {content.topics}
- Sentiment: {content.sentiment}
- Posted by: {content.author_name} ({content.author_title})

TASK:
Analyze whether this persona would engage with this content. Consider:
1. Topic alignment with persona's interests
2. Content format vs persona's preferences
3. Professional relevance
4. Emotional resonance
5. Social signals (if their network is engaging)

Provide your prediction in JSON format:
{{
    "will_engage": true/false,
    "probability": 0.0-1.0,
    "reasoning": "detailed explanation",
    "reaction_type": "like|comment|share|none",
    "comment": "predicted comment text if applicable"
}}
"""

PREDICTION_SYSTEM_PROMPT = """
You are an expert in social psychology and behavioral prediction. You analyze
personas and content to predict engagement behavior with high accuracy. Your
predictions should be realistic, considering human psychology, social dynamics,
and platform-specific behavior patterns.
"""


[TASK 3.1.4] Training Data Generation
──────────────────────────────────────
File: tinytroupe/training_data.py (new module)

Generate synthetic training data and collect real data:

SYNTHETIC DATA GENERATION:
class SyntheticDataGenerator:
    """Generate labeled training examples"""

    def generate_training_dataset(self, num_examples: int = 10000) -> List[TrainingExample]:
        """Generate synthetic persona-content-engagement triplets"""

        dataset = []

        # Generate diverse personas
        personas = self._generate_diverse_personas(num_examples // 100)

        # Generate diverse content
        content_items = self._generate_diverse_content(num_examples // 50)

        # Create combinations and label them
        for persona in personas:
            for content in content_items:
                # Use rule-based heuristics + LLM to label
                engagement = self._label_engagement(persona, content)

                dataset.append(TrainingExample(
                    persona=persona,
                    content=content,
                    engaged=engagement.engaged,
                    engagement_type=engagement.type,
                    timestamp=engagement.timestamp
                ))

        return dataset

    def _label_engagement(self, persona: TinyPerson, content: Content) -> EngagementLabel:
        """Use heuristics and LLM to create ground truth labels"""

        # Heuristic-based probability
        heuristic_prob = self._calculate_heuristic_probability(persona, content)

        # LLM-based labeling for quality
        if random.random() < 0.2:  # 20% LLM validation for quality
            llm_prediction = LLMPredictor().predict(persona, content)
            engaged = llm_prediction.will_engage
            engagement_type = llm_prediction.predicted_reaction_type
        else:
            engaged = random.random() < heuristic_prob
            engagement_type = self._sample_engagement_type(persona, content) if engaged else "none"

        return EngagementLabel(
            engaged=engaged,
            type=engagement_type,
            timestamp=datetime.now()
        )

REAL DATA INTEGRATION:
class RealDataCollector:
    """Collect real LinkedIn data for training (with user permission)"""

    def collect_user_engagement_history(self, linkedin_token: str) -> List[RealEngagement]:
        """Fetch user's LinkedIn engagement history via API"""
        # Use LinkedIn API to get:
        # - Posts user engaged with
        # - Posts user did NOT engage with (from feed)
        # - Network information
        # - Engagement types (like, comment, share)
        pass

    def augment_with_real_data(self, training_dataset: List[TrainingExample],
                                real_data: List[RealEngagement]) -> List[TrainingExample]:
        """Mix synthetic and real data for better training"""
        pass


[TASK 3.1.5] Accuracy Measurement & Validation
───────────────────────────────────────────────
File: tinytroupe/accuracy.py (new module)

Implement rigorous accuracy measurement:

class AccuracyValidator:
    """Validate prediction accuracy against ground truth"""

    def evaluate(self, predictor: EngagementPredictor,
                 test_data: List[TrainingExample]) -> AccuracyReport:
        """Comprehensive accuracy evaluation"""

        predictions = []
        ground_truth = []

        for example in test_data:
            pred_prob = predictor.predict(example.persona, example.content, example.network)
            pred_binary = pred_prob > 0.5

            predictions.append(pred_binary)
            ground_truth.append(example.engaged)

        return AccuracyReport(
            accuracy=accuracy_score(ground_truth, predictions),
            precision=precision_score(ground_truth, predictions),
            recall=recall_score(ground_truth, predictions),
            f1=f1_score(ground_truth, predictions),
            roc_auc=roc_auc_score(ground_truth, [p for p in predictions]),
            confusion_matrix=confusion_matrix(ground_truth, predictions),

            # Additional metrics
            calibration_curve=self._calculate_calibration(predictions, ground_truth),
            per_segment_accuracy=self._segment_analysis(test_data, predictions, ground_truth)
        )

    def _segment_analysis(self, test_data, predictions, ground_truth) -> Dict:
        """Analyze accuracy by segment (demographics, content type, etc.)"""
        segments = {
            "by_age_group": {},
            "by_content_type": {},
            "by_topic": {},
            "by_network_position": {}
        }

        # Calculate accuracy for each segment
        # This helps identify where model performs well/poorly

        return segments

    def compare_to_baseline(self, predictor: EngagementPredictor,
                           test_data: List[TrainingExample]) -> ComparisonReport:
        """Compare to baseline methods"""

        # Baseline 1: Random guessing
        random_predictions = [random.random() > 0.5 for _ in test_data]
        random_acc = accuracy_score([e.engaged for e in test_data], random_predictions)

        # Baseline 2: Majority class
        majority_predictions = [True] * len(test_data)  # Always predict engagement
        majority_acc = accuracy_score([e.engaged for e in test_data], majority_predictions)

        # Baseline 3: Simple LLM (GPT-4 without fine-tuning)
        llm_predictor = LLMPredictor()
        llm_predictions = [llm_predictor.predict(e.persona, e.content).will_engage
                          for e in test_data]
        llm_acc = accuracy_score([e.engaged for e in test_data], llm_predictions)

        # Our model
        our_report = self.evaluate(predictor, test_data)

        return ComparisonReport(
            our_accuracy=our_report.accuracy,
            random_accuracy=random_acc,
            majority_accuracy=majority_acc,
            llm_accuracy=llm_acc,
            improvement_over_random=our_report.accuracy - random_acc,
            improvement_over_llm=our_report.accuracy - llm_acc
        )

TARGET: 80-85% accuracy (matching or exceeding Artificial Societies' 83%)


================================================================================
SECTION 4: CONTENT GENERATION & VARIATION
================================================================================

4.1 CONTENT VARIANT GENERATION
────────────────────────────────────────────────────────────────────────────

OBJECTIVE:
Generate 100 variants of input content, optimized for different personas and
objectives. Match Artificial Societies' variant generation capability.

[TASK 4.1.1] Content Transformation Engine
───────────────────────────────────────────
File: tinytroupe/content_generation.py (new module)

Generate diverse content variations:

class ContentVariantGenerator:
    """Generate multiple variants of input content"""

    def __init__(self):
        self.llm = OpenAI()
        self.style_templates = self._load_style_templates()

    def generate_variants(self, original_content: str, num_variants: int = 100,
                         target_personas: List[TinyPerson] = None) -> List[ContentVariant]:
        """Generate diverse variants of content"""

        variants = []

        # Strategy 1: Tone variations (30%)
        tone_variants = self._generate_tone_variants(original_content,
                                                     num_variants=int(num_variants * 0.3))
        variants.extend(tone_variants)

        # Strategy 2: Length variations (20%)
        length_variants = self._generate_length_variants(original_content,
                                                         num_variants=int(num_variants * 0.2))
        variants.extend(length_variants)

        # Strategy 3: Format variations (20%)
        format_variants = self._generate_format_variants(original_content,
                                                         num_variants=int(num_variants * 0.2))
        variants.extend(format_variants)

        # Strategy 4: Persona-targeted variations (20%)
        if target_personas:
            persona_variants = self._generate_persona_targeted_variants(
                original_content, target_personas,
                num_variants=int(num_variants * 0.2)
            )
            variants.extend(persona_variants)

        # Strategy 5: Angle variations (10%)
        angle_variants = self._generate_angle_variants(original_content,
                                                       num_variants=int(num_variants * 0.1))
        variants.extend(angle_variants)

        # Ensure we have exactly num_variants
        variants = variants[:num_variants]

        return variants

    def _generate_tone_variants(self, content: str, num_variants: int) -> List[ContentVariant]:
        """Generate variants with different tones"""

        tones = [
            "professional and formal",
            "casual and friendly",
            "inspirational and motivational",
            "analytical and data-driven",
            "storytelling and narrative",
            "provocative and thought-provoking",
            "empathetic and supportive",
            "authoritative and expert",
            "humorous and lighthearted",
            "urgent and action-oriented"
        ]

        variants = []
        for tone in tones[:num_variants]:
            prompt = f"""
Rewrite the following content with a {tone} tone, while preserving the core message:

Original: {content}

Requirements:
- Maintain the key information and intent
- Adjust language and style to match the {tone} tone
- Keep similar length (±20%)
- Make it engaging and natural

Rewritten version:"""

            response = self.llm.chat.completions.create(
                model="gpt-4",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.8
            )

            variant_text = response.choices[0].message.content.strip()

            variants.append(ContentVariant(
                text=variant_text,
                strategy="tone_variation",
                parameters={"tone": tone},
                original_content=content
            ))

        return variants

    def _generate_length_variants(self, content: str, num_variants: int) -> List[ContentVariant]:
        """Generate variants of different lengths"""

        length_targets = [
            ("very_short", "tweet-length (280 characters or less)"),
            ("short", "2-3 sentences"),
            ("medium", "paragraph length"),
            ("long", "2-3 paragraphs with details"),
            ("comprehensive", "comprehensive explanation with examples")
        ]

        variants = []
        for length_name, length_desc in length_targets[:num_variants]:
            prompt = f"""
Rewrite the following content as a {length_desc}:

Original: {content}

Keep the core message but adjust the length to {length_desc}.

Rewritten version:"""

            response = self.llm.chat.completions.create(
                model="gpt-4",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.7
            )

            variant_text = response.choices[0].message.content.strip()

            variants.append(ContentVariant(
                text=variant_text,
                strategy="length_variation",
                parameters={"length": length_name},
                original_content=content
            ))

        return variants

    def _generate_format_variants(self, content: str, num_variants: int) -> List[ContentVariant]:
        """Generate variants with different formats"""

        formats = [
            "bullet points",
            "numbered list",
            "question and answer",
            "story format with hook",
            "comparison format (before/after, problem/solution)",
            "data-driven with statistics",
            "case study or example",
            "how-to guide",
            "controversial take",
            "personal experience"
        ]

        variants = []
        for format_type in formats[:num_variants]:
            prompt = f"""
Rewrite the following content using a {format_type} format:

Original: {content}

Format it as {format_type} while maintaining the core message.

Rewritten version:"""

            response = self.llm.chat.completions.create(
                model="gpt-4",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.8
            )

            variant_text = response.choices[0].message.content.strip()

            variants.append(ContentVariant(
                text=variant_text,
                strategy="format_variation",
                parameters={"format": format_type},
                original_content=content
            ))

        return variants

    def _generate_persona_targeted_variants(self, content: str,
                                           personas: List[TinyPerson],
                                           num_variants: int) -> List[ContentVariant]:
        """Generate variants optimized for specific personas"""

        variants = []
        selected_personas = random.sample(personas, min(num_variants, len(personas)))

        for persona in selected_personas:
            prompt = f"""
Rewrite the following content to appeal specifically to this persona:

Persona Profile:
- Age: {persona.age}
- Occupation: {persona.occupation}
- Interests: {", ".join(persona.interests)}
- Personality: {persona.personality_traits}
- Engagement preferences: {persona.engagement_patterns}

Original content: {content}

Rewrite to maximize engagement from this persona. Consider their interests,
communication style preferences, and what would resonate with them.

Rewritten version:"""

            response = self.llm.chat.completions.create(
                model="gpt-4",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.7
            )

            variant_text = response.choices[0].message.content.strip()

            variants.append(ContentVariant(
                text=variant_text,
                strategy="persona_targeted",
                parameters={"target_persona_id": persona.id},
                original_content=content
            ))

        return variants

    def _generate_angle_variants(self, content: str, num_variants: int) -> List[ContentVariant]:
        """Generate variants focusing on different angles/aspects"""

        # Extract key points from content
        key_points = self._extract_key_points(content)

        variants = []
        for i, point in enumerate(key_points[:num_variants]):
            prompt = f"""
Rewrite the following content, emphasizing this specific aspect: "{point}"

Original: {content}

Make this aspect the central focus while maintaining relevance to the overall message.

Rewritten version:"""

            response = self.llm.chat.completions.create(
                model="gpt-4",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.8
            )

            variant_text = response.choices[0].message.content.strip()

            variants.append(ContentVariant(
                text=variant_text,
                strategy="angle_variation",
                parameters={"focus_point": point},
                original_content=content
            ))

        return variants

    def _extract_key_points(self, content: str) -> List[str]:
        """Extract key points/aspects from content using LLM"""

        prompt = f"""
Extract the 5-10 key points or aspects from this content:

{content}

List them as brief phrases:"""

        response = self.llm.chat.completions.create(
            model="gpt-4",
            messages=[{"role": "user", "content": prompt}],
            temperature=0.3
        )

        points = response.choices[0].message.content.strip().split('\n')
        return [p.strip('- ').strip() for p in points if p.strip()]


[TASK 4.1.2] Variant Optimization & Ranking
────────────────────────────────────────────
File: tinytroupe/variant_optimizer.py (new module)

Rank and select best variants for specific objectives:

class VariantOptimizer:
    """Optimize and rank content variants"""

    def __init__(self, predictor: EngagementPredictor):
        self.predictor = predictor

    def rank_variants_for_audience(self, variants: List[ContentVariant],
                                   target_personas: List[TinyPerson],
                                   network: NetworkTopology,
                                   objective: str = "maximize_engagement") -> List[RankedVariant]:
        """Rank variants by predicted performance"""

        ranked = []

        for variant in variants:
            # Predict engagement for each persona
            engagement_probs = []
            for persona in target_personas:
                prob = self.predictor.predict(
                    persona,
                    Content(text=variant.text),
                    network
                )
                engagement_probs.append(prob)

            # Calculate aggregate metrics based on objective
            if objective == "maximize_engagement":
                score = np.mean(engagement_probs)
            elif objective == "maximize_reach":
                # Weight by persona influence
                weighted_probs = [
                    prob * persona.influence_metrics.reach
                    for prob, persona in zip(engagement_probs, target_personas)
                ]
                score = np.mean(weighted_probs)
            elif objective == "maximize_conversions":
                # Focus on high-intent personas
                high_intent_personas = [p for p in target_personas if p.intent_score > 0.7]
                relevant_probs = [engagement_probs[i] for i, p in enumerate(target_personas)
                                if p in high_intent_personas]
                score = np.mean(relevant_probs) if relevant_probs else 0.0

            ranked.append(RankedVariant(
                variant=variant,
                score=score,
                engagement_probs=engagement_probs,
                predicted_reach=self._estimate_reach(variant, target_personas, network),
                predicted_engagement_count=int(sum(engagement_probs))
            ))

        # Sort by score
        ranked.sort(key=lambda x: x.score, reverse=True)

        return ranked

    def _estimate_reach(self, variant: ContentVariant,
                       personas: List[TinyPerson],
                       network: NetworkTopology) -> int:
        """Estimate total reach through cascade simulation"""

        # Simulate cascade for top engaging personas
        engaging_personas = [p for p, prob in zip(personas, engagement_probs)
                           if prob > 0.5]

        if not engaging_personas:
            return 0

        propagator = InfluencePropagator(network)
        result = propagator.propagate(
            [p.id for p in engaging_personas[:10]],  # Top 10 seed nodes
            Content(text=variant.text)
        )

        return result.total_reach

    def generate_variant_report(self, ranked_variants: List[RankedVariant],
                               top_k: int = 10) -> VariantReport:
        """Generate report comparing top variants"""

        top_variants = ranked_variants[:top_k]

        return VariantReport(
            top_variants=top_variants,
            best_variant=top_variants[0],
            score_distribution=[v.score for v in ranked_variants],
            strategy_performance=self._analyze_strategy_performance(ranked_variants),
            recommendations=self._generate_recommendations(top_variants)
        )

    def _analyze_strategy_performance(self, variants: List[RankedVariant]) -> Dict:
        """Analyze which generation strategies work best"""

        strategy_scores = defaultdict(list)
        for ranked_variant in variants:
            strategy = ranked_variant.variant.strategy
            strategy_scores[strategy].append(ranked_variant.score)

        return {
            strategy: {
                "avg_score": np.mean(scores),
                "top_10_percentage": sum(1 for v in variants[:10]
                                       if v.variant.strategy == strategy) / 10
            }
            for strategy, scores in strategy_scores.items()
        }


[TASK 4.1.3] A/B Test Simulation
─────────────────────────────────
File: tinytroupe/ab_testing.py (new module)

Simulate A/B tests between content variants:

class ABTestSimulator:
    """Simulate A/B tests to compare content variants"""

    def run_test(self, variant_a: ContentVariant, variant_b: ContentVariant,
                 audience: List[TinyPerson], network: NetworkTopology,
                 duration_days: int = 7) -> ABTestResult:
        """Simulate A/B test between two variants"""

        # Split audience randomly
        split_point = len(audience) // 2
        audience_a = audience[:split_point]
        audience_b = audience[split_point:]

        # Simulate engagement for each group
        results_a = self._simulate_engagement(variant_a, audience_a, network, duration_days)
        results_b = self._simulate_engagement(variant_b, audience_b, network, duration_days)

        # Statistical comparison
        significance = self._calculate_significance(results_a, results_b)

        return ABTestResult(
            variant_a=variant_a,
            variant_b=variant_b,
            engagement_rate_a=results_a.engagement_rate,
            engagement_rate_b=results_b.engagement_rate,
            winner="A" if results_a.engagement_rate > results_b.engagement_rate else "B",
            lift_percentage=self._calculate_lift(results_a, results_b),
            statistical_significance=significance,
            confidence_level=self._calculate_confidence(significance)
        )

    def _simulate_engagement(self, variant: ContentVariant,
                            audience: List[TinyPerson],
                            network: NetworkTopology,
                            duration_days: int) -> EngagementResults:
        """Simulate engagement over time"""

        engaged_count = 0
        engagements = []

        for persona in audience:
            # Predict engagement
            prob = self.predictor.predict(persona, Content(text=variant.text), network)
            engaged = random.random() < prob

            if engaged:
                engaged_count += 1
                engagement_time = random.randint(0, duration_days * 24 * 60)  # Random time in minutes
                engagements.append(Engagement(
                    persona_id=persona.id,
                    timestamp=engagement_time,
                    type=self._sample_engagement_type(persona)
                ))

        return EngagementResults(
            total_views=len(audience),
            total_engagements=engaged_count,
            engagement_rate=engaged_count / len(audience),
            engagements=engagements,
            cascade_reach=self._simulate_cascade(variant, engagements, network)
        )

    def _calculate_significance(self, results_a: EngagementResults,
                                results_b: EngagementResults) -> float:
        """Calculate statistical significance using chi-square test"""

        from scipy.stats import chi2_contingency

        contingency_table = [
            [results_a.total_engagements, results_a.total_views - results_a.total_engagements],
            [results_b.total_engagements, results_b.total_views - results_b.total_engagements]
        ]

        chi2, p_value, dof, expected = chi2_contingency(contingency_table)

        return p_value


================================================================================
SECTION 5: LINKEDIN INTEGRATION
================================================================================

5.1 LINKEDIN API INTEGRATION
────────────────────────────────────────────────────────────────────────────

OBJECTIVE:
Integrate with LinkedIn to fetch user's network, analyze audience, and enable
post testing. This is CRITICAL for the REACH product.

[TASK 5.1.1] LinkedIn OAuth & Authentication
─────────────────────────────────────────────
File: tinytroupe/integrations/linkedin_auth.py (new module)

Implement LinkedIn OAuth 2.0 flow:

class LinkedInAuth:
    """Handle LinkedIn OAuth authentication"""

    def __init__(self, client_id: str, client_secret: str, redirect_uri: str):
        self.client_id = client_id
        self.client_secret = client_secret
        self.redirect_uri = redirect_uri
        self.base_url = "https://www.linkedin.com/oauth/v2"
        self.api_base = "https://api.linkedin.com/v2"

    def get_authorization_url(self, state: str) -> str:
        """Generate LinkedIn authorization URL"""

        scopes = [
            "r_liteprofile",
            "r_emailaddress",
            "w_member_social",
            "r_organization_social",
            "rw_organization_admin"
        ]

        params = {
            "response_type": "code",
            "client_id": self.client_id,
            "redirect_uri": self.redirect_uri,
            "state": state,
            "scope": " ".join(scopes)
        }

        return f"{self.base_url}/authorization?" + urlencode(params)

    def exchange_code_for_token(self, code: str) -> AccessToken:
        """Exchange authorization code for access token"""

        data = {
            "grant_type": "authorization_code",
            "code": code,
            "redirect_uri": self.redirect_uri,
            "client_id": self.client_id,
            "client_secret": self.client_secret
        }

        response = requests.post(f"{self.base_url}/accessToken", data=data)
        response.raise_for_status()

        token_data = response.json()

        return AccessToken(
            access_token=token_data["access_token"],
            expires_in=token_data["expires_in"],
            refresh_token=token_data.get("refresh_token"),
            created_at=datetime.now()
        )

    def refresh_access_token(self, refresh_token: str) -> AccessToken:
        """Refresh expired access token"""

        data = {
            "grant_type": "refresh_token",
            "refresh_token": refresh_token,
            "client_id": self.client_id,
            "client_secret": self.client_secret
        }

        response = requests.post(f"{self.base_url}/accessToken", data=data)
        response.raise_for_status()

        token_data = response.json()

        return AccessToken(
            access_token=token_data["access_token"],
            expires_in=token_data["expires_in"],
            refresh_token=token_data.get("refresh_token"),
            created_at=datetime.now()
        )


[TASK 5.1.2] LinkedIn Profile & Network Fetcher
────────────────────────────────────────────────
File: tinytroupe/integrations/linkedin_api.py (new module)

Fetch user profile and network data:

class LinkedInAPI:
    """LinkedIn API client for fetching user data"""

    def __init__(self, access_token: str):
        self.access_token = access_token
        self.base_url = "https://api.linkedin.com/v2"
        self.headers = {
            "Authorization": f"Bearer {access_token}",
            "Content-Type": "application/json"
        }

    def get_user_profile(self) -> LinkedInProfile:
        """Fetch authenticated user's profile"""

        # Basic profile
        profile_response = requests.get(
            f"{self.base_url}/me",
            headers=self.headers
        )
        profile_response.raise_for_status()
        profile_data = profile_response.json()

        # Email
        email_response = requests.get(
            f"{self.base_url}/emailAddress?q=members&projection=(elements*(handle~))",
            headers=self.headers
        )
        email_data = email_response.json()

        return LinkedInProfile(
            id=profile_data["id"],
            first_name=profile_data["localizedFirstName"],
            last_name=profile_data["localizedLastName"],
            headline=profile_data.get("headline", ""),
            email=email_data["elements"][0]["handle~"]["emailAddress"],
            profile_picture=profile_data.get("profilePicture", {})
        )

    def get_connections(self, start: int = 0, count: int = 100) -> List[LinkedInConnection]:
        """Fetch user's connections"""

        # Note: LinkedIn API has limited connection access
        # May need to use alternative methods or partner API access

        url = f"{self.base_url}/connections"
        params = {"start": start, "count": count}

        response = requests.get(url, headers=self.headers, params=params)
        response.raise_for_status()

        data = response.json()

        connections = []
        for conn in data.get("elements", []):
            connections.append(LinkedInConnection(
                id=conn["id"],
                first_name=conn["localizedFirstName"],
                last_name=conn["localizedLastName"],
                headline=conn.get("headline", ""),
                location=conn.get("location", {}),
                industry=conn.get("industry", "")
            ))

        return connections

    def get_user_posts(self, author_id: str, count: int = 50) -> List[LinkedInPost]:
        """Fetch user's posts and shares"""

        url = f"{self.base_url}/shares"
        params = {
            "q": "author",
            "author": f"urn:li:person:{author_id}",
            "count": count,
            "sortBy": "LAST_MODIFIED"
        }

        response = requests.get(url, headers=self.headers, params=params)
        response.raise_for_status()

        data = response.json()

        posts = []
        for share in data.get("elements", []):
            posts.append(LinkedInPost(
                id=share["id"],
                text=share["text"]["text"],
                created_at=datetime.fromtimestamp(share["created"]["time"] / 1000),
                visibility=share["visibility"]["code"],
                engagement=self._fetch_post_engagement(share["id"])
            ))

        return posts

    def _fetch_post_engagement(self, post_id: str) -> PostEngagement:
        """Fetch engagement metrics for a post"""

        # Social actions (likes, comments, shares)
        url = f"{self.base_url}/socialActions/{post_id}"

        response = requests.get(url, headers=self.headers)
        response.raise_for_status()

        data = response.json()

        return PostEngagement(
            likes=data.get("likesSummary", {}).get("totalLikes", 0),
            comments=data.get("commentsSummary", {}).get("totalComments", 0),
            shares=data.get("sharesSummary", {}).get("totalShares", 0),
            impressions=data.get("impressions", 0)
        )

    def get_post_analytics(self, post_id: str) -> PostAnalytics:
        """Get detailed analytics for a post (requires elevated permissions)"""

        # This may require LinkedIn Marketing API access
        url = f"{self.base_url}/organizationalEntityShareStatistics"
        params = {"q": "organizationalEntity", "shares": [f"urn:li:share:{post_id}"]}

        response = requests.get(url, headers=self.headers, params=params)

        if response.status_code == 200:
            data = response.json()
            return self._parse_analytics(data)

        return None


[TASK 5.1.3] Audience Analysis & Persona Extraction
────────────────────────────────────────────────────
File: tinytroupe/integrations/linkedin_audience.py (new module)

Analyze LinkedIn network and create personas:

class LinkedInAudienceAnalyzer:
    """Analyze LinkedIn audience and create persona models"""

    def __init__(self, linkedin_api: LinkedInAPI):
        self.api = linkedin_api
        self.persona_factory = TinyPersonFactory()

    def analyze_audience(self, user_profile: LinkedInProfile) -> AudienceAnalysis:
        """Comprehensive audience analysis"""

        # Fetch connections
        connections = self.api.get_connections(count=500)

        # Fetch user's posts and engagement
        posts = self.api.get_user_posts(user_profile.id, count=100)

        # Analyze demographics
        demographics = self._analyze_demographics(connections)

        # Analyze engagement patterns
        engagement_patterns = self._analyze_engagement(posts)

        # Identify key influencers in network
        influencers = self._identify_influencers(connections, posts)

        # Detect communities
        communities = self._detect_communities(connections)

        return AudienceAnalysis(
            total_connections=len(connections),
            demographics=demographics,
            engagement_patterns=engagement_patterns,
            influencers=influencers,
            communities=communities,
            avg_engagement_rate=np.mean([p.engagement.engagement_rate for p in posts])
        )

    def create_persona_from_connection(self, connection: LinkedInConnection) -> TinyPerson:
        """Convert LinkedIn connection to TinyPerson"""

        # Extract features
        description = f"""
Professional with headline: {connection.headline}
Industry: {connection.industry}
Location: {connection.location}
"""

        # Generate persona with LLM enrichment
        persona = self.persona_factory.generate_person(
            description=description,
            additional_context={
                "linkedin_id": connection.id,
                "professional_network": True
            }
        )

        # Enhance with LinkedIn-specific traits
        persona.social_platform = "LinkedIn"
        persona.professional_interests = self._infer_interests(connection)
        persona.engagement_patterns = self._infer_engagement_patterns(connection)

        return persona

    def create_audience_personas(self, connections: List[LinkedInConnection],
                                num_personas: int = 100) -> List[TinyPerson]:
        """Create representative personas from connections"""

        # Cluster connections by similarity
        clusters = self._cluster_connections(connections, num_clusters=num_personas)

        # Create one persona per cluster (representative)
        personas = []
        for cluster in clusters:
            representative = self._select_representative(cluster)
            persona = self.create_persona_from_connection(representative)

            # Add cluster statistics to persona
            persona.cluster_size = len(cluster)
            persona.represents_count = len(cluster)

            personas.append(persona)

        return personas

    def _cluster_connections(self, connections: List[LinkedInConnection],
                           num_clusters: int) -> List[List[LinkedInConnection]]:
        """Cluster connections by similarity"""

        # Feature extraction
        features = []
        for conn in connections:
            features.append(self._extract_connection_features(conn))

        features = np.array(features)

        # K-means clustering
        from sklearn.cluster import KMeans
        kmeans = KMeans(n_clusters=num_clusters, random_state=42)
        labels = kmeans.fit_predict(features)

        # Group by cluster
        clusters = defaultdict(list)
        for conn, label in zip(connections, labels):
            clusters[label].append(conn)

        return list(clusters.values())

    def _extract_connection_features(self, connection: LinkedInConnection) -> np.ndarray:
        """Extract features for clustering"""

        # Features: industry (one-hot), location (one-hot), seniority level, etc.
        # For simplicity, use embeddings

        text = f"{connection.headline} {connection.industry} {connection.location}"
        embedding = self._get_text_embedding(text)

        return embedding

    def _get_text_embedding(self, text: str) -> np.ndarray:
        """Get text embedding using OpenAI or sentence-transformers"""

        from sentence_transformers import SentenceTransformer
        model = SentenceTransformer('all-MiniLM-L6-v2')
        embedding = model.encode(text)

        return embedding


[TASK 5.1.4] Post Simulation & Testing
───────────────────────────────────────
File: tinytroupe/integrations/linkedin_simulator.py (new module)

Simulate how a LinkedIn post will perform:

class LinkedInPostSimulator:
    """Simulate LinkedIn post performance before publishing"""

    def __init__(self, predictor: EngagementPredictor):
        self.predictor = predictor

    def simulate_post(self, post_content: str,
                     audience_personas: List[TinyPerson],
                     network: NetworkTopology) -> PostSimulationResult:
        """Simulate post performance on LinkedIn"""

        # Create Content object
        content = Content(
            text=post_content,
            platform="LinkedIn",
            content_type="post",
            timestamp=datetime.now()
        )

        # Predict engagement for each persona
        engagement_predictions = []
        for persona in audience_personas:
            prediction = self.predictor.predict(persona, content, network)
            engagement_predictions.append(EngagementPrediction(
                persona_id=persona.id,
                will_engage=prediction.engagement_probability > 0.5,
                engagement_probability=prediction.engagement_probability,
                predicted_reaction=prediction.predicted_reaction,
                predicted_comment=prediction.predicted_comment
            ))

        # Aggregate metrics
        total_reach = len(audience_personas)
        expected_likes = sum(1 for p in engagement_predictions if p.predicted_reaction == "like")
        expected_comments = sum(1 for p in engagement_predictions if p.predicted_reaction == "comment")
        expected_shares = sum(1 for p in engagement_predictions if p.predicted_reaction == "share")

        # Simulate viral spread
        viral_reach = self._simulate_viral_spread(
            engagement_predictions,
            network,
            content
        )

        # Calculate engagement rate
        total_engagements = expected_likes + expected_comments + expected_shares
        engagement_rate = total_engagements / total_reach if total_reach > 0 else 0.0

        return PostSimulationResult(
            post_content=post_content,
            direct_reach=total_reach,
            viral_reach=viral_reach,
            total_reach=total_reach + viral_reach,
            expected_likes=expected_likes,
            expected_comments=expected_comments,
            expected_shares=expected_shares,
            engagement_rate=engagement_rate,
            engagement_predictions=engagement_predictions,
            confidence_score=self._calculate_confidence(engagement_predictions)
        )

    def _simulate_viral_spread(self, initial_engagements: List[EngagementPrediction],
                              network: NetworkTopology,
                              content: Content) -> int:
        """Simulate secondary engagement through shares"""

        # Find personas who will share
        sharers = [p for p in initial_engagements if p.predicted_reaction == "share"]

        if not sharers:
            return 0

        # Simulate cascade from sharers
        propagator = InfluencePropagator(network)
        result = propagator.propagate(
            [p.persona_id for p in sharers],
            content
        )

        # Exclude original audience
        viral_reach = result.total_reach - len(initial_engagements)

        return max(0, viral_reach)

    def compare_post_variants(self, variants: List[str],
                            audience: List[TinyPerson],
                            network: NetworkTopology) -> ComparisonResult:
        """Compare multiple post variants"""

        results = []
        for variant_text in variants:
            result = self.simulate_post(variant_text, audience, network)
            results.append(result)

        # Sort by engagement rate
        results.sort(key=lambda x: x.engagement_rate, reverse=True)

        return ComparisonResult(
            best_variant=results[0],
            all_results=results,
            winner_improvement=self._calculate_improvement(results[0], results[-1])
        )


================================================================================
SECTION 6: SIMULATION ORCHESTRATION
================================================================================

6.1 SIMULATION ENGINE
────────────────────────────────────────────────────────────────────────────

OBJECTIVE:
Orchestrate complex simulations with multiple personas, content, and network
effects. This is the core execution engine.

[TASK 6.1.1] Enhanced TinyWorld
────────────────────────────────
File: tinytroupe/environment.py (extend TinyWorld)

Enhance TinyWorld for social simulations:

class SocialTinyWorld(TinyWorld):
    """Extended TinyWorld with social network capabilities"""

    def __init__(self, name: str, network: NetworkTopology = None):
        super().__init__(name)
        self.network = network or NetworkTopology()
        self.content_items = []
        self.simulation_history = []
        self.time_step = 0

    def add_content(self, content: Content) -> None:
        """Add content to the world for personas to interact with"""
        self.content_items.append(content)
        self.broadcast_to_agents(f"New content available: {content.text[:100]}...")

    def simulate_content_spread(self, content: Content,
                               initial_viewers: List[str],
                               max_steps: int = 10) -> SimulationResult:
        """Simulate how content spreads through the network"""

        result = SimulationResult(
            content=content,
            start_time=datetime.now()
        )

        # Initialize: Initial viewers see content
        viewed = set(initial_viewers)
        engaged = set()

        for step in range(max_steps):
            self.time_step = step
            new_viewers = set()

            # Each viewer decides whether to engage
            for viewer_id in viewed - engaged:
                persona = self.network.nodes[viewer_id]

                # Simulate persona viewing and deciding
                decision = self._simulate_engagement_decision(persona, content, step)

                if decision.engaged:
                    engaged.add(viewer_id)
                    result.add_engagement(viewer_id, decision.engagement_type, step)

                    # If shared, content spreads to their connections
                    if decision.engagement_type == "share":
                        neighbors = self.network.get_neighbors(viewer_id)
                        new_viewers.update([n.id for n in neighbors])

            # Add new viewers
            viewed.update(new_viewers)

            # Track metrics for this step
            result.add_step_metrics(step, len(viewed), len(engaged))

            # Stop if no new activity
            if not new_viewers and not any(v not in engaged for v in viewed):
                break

        result.finalize(datetime.now())
        self.simulation_history.append(result)

        return result

    def _simulate_engagement_decision(self, persona: TinyPerson,
                                     content: Content,
                                     time_step: int) -> EngagementDecision:
        """Simulate a persona's decision to engage with content"""

        # Gather context
        network_influence = self._calculate_network_influence(persona, content)
        content_relevance = persona.get_content_affinity(content)

        # Generate engagement probability
        base_prob = persona.calculate_engagement_probability(content)

        # Adjust for network influence
        adjusted_prob = base_prob * (1 + 0.5 * network_influence)

        # Adjust for time decay (content gets stale)
        time_decay = 0.9 ** time_step
        final_prob = adjusted_prob * time_decay

        # Make decision
        engaged = random.random() < final_prob

        if engaged:
            # Determine engagement type
            engagement_type = self._sample_engagement_type(persona, content)

            # Generate comment if applicable
            comment = None
            if engagement_type == "comment":
                comment = self._generate_comment(persona, content)

            return EngagementDecision(
                engaged=True,
                engagement_type=engagement_type,
                comment=comment,
                probability=final_prob
            )

        return EngagementDecision(engaged=False, probability=final_prob)

    def _calculate_network_influence(self, persona: TinyPerson,
                                    content: Content) -> float:
        """Calculate social influence on engagement decision"""

        # Check if persona's connections have engaged
        connections = self.network.get_neighbors(persona.id)
        engaged_connections = [
            c for c in connections
            if self._has_engaged_with_content(c.id, content)
        ]

        if not connections:
            return 0.0

        # Weight by connection strength
        influence_sum = sum(
            self.network.get_connection_strength(persona.id, c.id)
            for c in engaged_connections
        )

        # Normalize
        influence = influence_sum / len(connections)

        return influence

    def run_parallel_simulations(self, scenarios: List[Scenario]) -> List[SimulationResult]:
        """Run multiple simulation scenarios in parallel"""

        from concurrent.futures import ThreadPoolExecutor

        with ThreadPoolExecutor(max_workers=10) as executor:
            futures = [
                executor.submit(self._run_scenario, scenario)
                for scenario in scenarios
            ]

            results = [future.result() for future in futures]

        return results

    def _run_scenario(self, scenario: Scenario) -> SimulationResult:
        """Run a single simulation scenario"""

        # Reset state for this scenario
        world_copy = self._create_world_copy()

        # Run simulation
        result = world_copy.simulate_content_spread(
            scenario.content,
            scenario.initial_viewers,
            scenario.max_steps
        )

        return result


[TASK 6.1.2] Simulation Manager
────────────────────────────────
File: tinytroupe/simulation_manager.py (new module)

High-level simulation orchestration:

class SimulationManager:
    """Manages simulation lifecycle and execution"""

    def __init__(self):
        self.worlds = {}
        self.predictor = EngagementPredictor()
        self.variant_generator = ContentVariantGenerator()

    def create_simulation(self, config: SimulationConfig) -> Simulation:
        """Create a new simulation from configuration"""

        # Generate or load personas
        if config.use_linkedin_audience:
            personas = self._create_personas_from_linkedin(config.linkedin_token)
        else:
            personas = self._generate_synthetic_personas(config.persona_count)

        # Generate network
        network = self._create_network(personas, config.network_type)

        # Create world
        world = SocialTinyWorld(config.name, network)
        for persona in personas:
            world.add_agent(persona)

        # Create simulation object
        simulation = Simulation(
            id=generate_id(),
            config=config,
            world=world,
            personas=personas,
            network=network,
            status="ready"
        )

        self.worlds[simulation.id] = simulation

        return simulation

    def run_simulation(self, simulation_id: str, content: Content,
                      mode: str = "full") -> SimulationResult:
        """Run a simulation"""

        simulation = self.worlds[simulation_id]
        simulation.status = "running"

        try:
            if mode == "full":
                # Run full cascade simulation
                result = simulation.world.simulate_content_spread(
                    content,
                    initial_viewers=self._select_initial_viewers(simulation),
                    max_steps=10
                )
            elif mode == "fast":
                # Use prediction only (no cascade)
                result = self._fast_prediction_simulation(simulation, content)
            elif mode == "variants":
                # Generate and test multiple variants
                result = self._variant_simulation(simulation, content)

            simulation.status = "completed"
            simulation.last_result = result

            return result

        except Exception as e:
            simulation.status = "failed"
            simulation.error = str(e)
            raise

    def _fast_prediction_simulation(self, simulation: Simulation,
                                   content: Content) -> SimulationResult:
        """Fast simulation using predictions only"""

        result = SimulationResult(content=content, start_time=datetime.now())

        for persona in simulation.personas:
            prob = self.predictor.predict(persona, content, simulation.network)

            if prob > 0.5:
                result.add_engagement(
                    persona.id,
                    self._sample_engagement_type(persona, content),
                    time_step=0
                )

        result.finalize(datetime.now())
        return result

    def _variant_simulation(self, simulation: Simulation,
                          content: Content) -> VariantSimulationResult:
        """Generate and test multiple content variants"""

        # Generate variants
        variants = self.variant_generator.generate_variants(
            content.text,
            num_variants=100,
            target_personas=simulation.personas[:20]  # Use sample for targeting
        )

        # Test each variant
        results = []
        for variant in variants:
            variant_content = Content(text=variant.text, **content.metadata)
            result = self._fast_prediction_simulation(simulation, variant_content)
            results.append((variant, result))

        # Rank by performance
        results.sort(key=lambda x: x[1].engagement_rate, reverse=True)

        return VariantSimulationResult(
            original_content=content,
            variants=variants,
            results=results,
            best_variant=results[0]
        )


[TASK 6.1.3] Simulation Caching & Optimization
───────────────────────────────────────────────
File: tinytroupe/simulation_cache.py (new module)

Implement caching to speed up simulations:

class SimulationCache:
    """Cache simulation results for faster repeated simulations"""

    def __init__(self, redis_client: redis.Redis):
        self.redis = redis_client
        self.ttl = 3600 * 24 * 7  # 1 week

    def get_cached_prediction(self, persona_id: str, content_hash: str) -> Optional[float]:
        """Get cached engagement prediction"""

        key = f"prediction:{persona_id}:{content_hash}"
        cached = self.redis.get(key)

        if cached:
            return float(cached)

        return None

    def cache_prediction(self, persona_id: str, content_hash: str,
                        probability: float) -> None:
        """Cache engagement prediction"""

        key = f"prediction:{persona_id}:{content_hash}"
        self.redis.setex(key, self.ttl, probability)

    def get_cached_simulation(self, config_hash: str) -> Optional[SimulationResult]:
        """Get cached simulation result"""

        key = f"simulation:{config_hash}"
        cached = self.redis.get(key)

        if cached:
            return pickle.loads(cached)

        return None

    def cache_simulation(self, config_hash: str, result: SimulationResult) -> None:
        """Cache simulation result"""

        key = f"simulation:{config_hash}"
        self.redis.setex(key, self.ttl, pickle.dumps(result))

    @staticmethod
    def hash_content(content: Content) -> str:
        """Generate hash for content"""

        content_str = f"{content.text}:{content.platform}:{content.content_type}"
        return hashlib.sha256(content_str.encode()).hexdigest()

    @staticmethod
    def hash_config(config: SimulationConfig) -> str:
        """Generate hash for simulation configuration"""

        config_str = json.dumps(config.to_dict(), sort_keys=True)
        return hashlib.sha256(config_str.encode()).hexdigest()


================================================================================
SECTION 7: API & BACKEND INFRASTRUCTURE
================================================================================

7.1 REST API
────────────────────────────────────────────────────────────────────────────

OBJECTIVE:
Build production-ready REST API for frontend integration and external access.

[TASK 7.1.1] FastAPI Application Structure
───────────────────────────────────────────
File: api/main.py (new file)

Create main FastAPI application:

```python
from fastapi import FastAPI, HTTPException, Depends, BackgroundTasks
from fastapi.middleware.cors import CORSMiddleware
from fastapi.security import OAuth2PasswordBearer
import uvicorn

app = FastAPI(
    title="TinyTroupe Simulation API",
    description="API for running persona-based social simulations",
    version="1.0.0"
)

# CORS configuration
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Configure appropriately for production
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Authentication
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")

# Global dependencies
simulation_manager = SimulationManager()
linkedin_service = LinkedInService()
predictor = EngagementPredictor()

# Import routers
from api.routers import simulations, personas, content, linkedin, analytics

app.include_router(simulations.router, prefix="/api/v1/simulations", tags=["simulations"])
app.include_router(personas.router, prefix="/api/v1/personas", tags=["personas"])
app.include_router(content.router, prefix="/api/v1/content", tags=["content"])
app.include_router(linkedin.router, prefix="/api/v1/linkedin", tags=["linkedin"])
app.include_router(analytics.router, prefix="/api/v1/analytics", tags=["analytics"])

@app.get("/health")
async def health_check():
    return {"status": "healthy", "version": "1.0.0"}

if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8000)
```


[TASK 7.1.2] Simulation Endpoints
──────────────────────────────────
File: api/routers/simulations.py (new file)

Implement simulation management endpoints:

```python
from fastapi import APIRouter, HTTPException, BackgroundTasks
from typing import List
from pydantic import BaseModel

router = APIRouter()

# Request/Response models
class CreateSimulationRequest(BaseModel):
    name: str
    persona_count: int = 100
    network_type: str = "scale_free"
    use_linkedin_audience: bool = False
    linkedin_token: Optional[str] = None

class RunSimulationRequest(BaseModel):
    content: str
    mode: str = "full"  # full, fast, variants
    initial_viewers: Optional[List[str]] = None

class SimulationResponse(BaseModel):
    id: str
    name: str
    status: str
    persona_count: int
    network_metrics: Dict
    created_at: datetime

class SimulationResultResponse(BaseModel):
    simulation_id: str
    content: str
    total_reach: int
    engagement_rate: float
    expected_likes: int
    expected_comments: int
    expected_shares: int
    cascade_depth: int
    execution_time_seconds: float

# Endpoints
@router.post("/", response_model=SimulationResponse)
async def create_simulation(request: CreateSimulationRequest,
                           user_id: str = Depends(get_current_user)):
    """Create a new simulation environment"""

    config = SimulationConfig(
        name=request.name,
        persona_count=request.persona_count,
        network_type=request.network_type,
        use_linkedin_audience=request.use_linkedin_audience,
        linkedin_token=request.linkedin_token,
        user_id=user_id
    )

    simulation = simulation_manager.create_simulation(config)

    return SimulationResponse(
        id=simulation.id,
        name=simulation.config.name,
        status=simulation.status,
        persona_count=len(simulation.personas),
        network_metrics=simulation.network.get_metrics(),
        created_at=simulation.created_at
    )

@router.get("/", response_model=List[SimulationResponse])
async def list_simulations(user_id: str = Depends(get_current_user)):
    """List all simulations for the user"""

    simulations = simulation_manager.get_user_simulations(user_id)

    return [
        SimulationResponse(
            id=sim.id,
            name=sim.config.name,
            status=sim.status,
            persona_count=len(sim.personas),
            network_metrics=sim.network.get_metrics(),
            created_at=sim.created_at
        )
        for sim in simulations
    ]

@router.get("/{simulation_id}", response_model=SimulationResponse)
async def get_simulation(simulation_id: str,
                         user_id: str = Depends(get_current_user)):
    """Get simulation details"""

    simulation = simulation_manager.get_simulation(simulation_id, user_id)

    if not simulation:
        raise HTTPException(status_code=404, detail="Simulation not found")

    return SimulationResponse(
        id=simulation.id,
        name=simulation.config.name,
        status=simulation.status,
        persona_count=len(simulation.personas),
        network_metrics=simulation.network.get_metrics(),
        created_at=simulation.created_at
    )

@router.post("/{simulation_id}/run", response_model=SimulationResultResponse)
async def run_simulation(simulation_id: str,
                        request: RunSimulationRequest,
                        background_tasks: BackgroundTasks,
                        user_id: str = Depends(get_current_user)):
    """Run a simulation with given content"""

    content = Content(text=request.content, platform="linkedin")

    # Run simulation
    result = simulation_manager.run_simulation(
        simulation_id,
        content,
        mode=request.mode
    )

    # Store result asynchronously
    background_tasks.add_task(store_simulation_result, result)

    return SimulationResultResponse(
        simulation_id=simulation_id,
        content=content.text,
        total_reach=result.total_reach,
        engagement_rate=result.engagement_rate,
        expected_likes=result.expected_likes,
        expected_comments=result.expected_comments,
        expected_shares=result.expected_shares,
        cascade_depth=result.cascade_depth,
        execution_time_seconds=result.execution_time
    )

@router.delete("/{simulation_id}")
async def delete_simulation(simulation_id: str,
                           user_id: str = Depends(get_current_user)):
    """Delete a simulation"""

    success = simulation_manager.delete_simulation(simulation_id, user_id)

    if not success:
        raise HTTPException(status_code=404, detail="Simulation not found")

    return {"message": "Simulation deleted successfully"}
```


[TASK 7.1.3] Content & Variant Endpoints
─────────────────────────────────────────
File: api/routers/content.py (new file)

Implement content generation and testing endpoints:

```python
router = APIRouter()

@router.post("/variants")
async def generate_variants(request: GenerateVariantsRequest,
                           user_id: str = Depends(get_current_user)):
    """Generate content variants"""

    generator = ContentVariantGenerator()

    variants = generator.generate_variants(
        request.content,
        num_variants=request.num_variants,
        target_personas=request.target_personas
    )

    return {"variants": [v.to_dict() for v in variants]}

@router.post("/test")
async def test_content(request: TestContentRequest,
                      user_id: str = Depends(get_current_user)):
    """Test content against audience"""

    # Get or create simulation
    simulation = simulation_manager.get_or_create_default_simulation(user_id)

    # Run prediction
    result = simulation_manager.run_simulation(
        simulation.id,
        Content(text=request.content),
        mode="fast"
    )

    return {
        "engagement_rate": result.engagement_rate,
        "expected_reach": result.total_reach,
        "predicted_performance": result.performance_score,
        "recommendations": result.recommendations
    }

@router.post("/compare")
async def compare_variants(request: CompareVariantsRequest,
                          user_id: str = Depends(get_current_user)):
    """Compare multiple content variants"""

    simulation = simulation_manager.get_simulation(request.simulation_id, user_id)

    results = []
    for variant in request.variants:
        result = simulation_manager.run_simulation(
            simulation.id,
            Content(text=variant),
            mode="fast"
        )
        results.append(result)

    # Rank by performance
    results.sort(key=lambda x: x.engagement_rate, reverse=True)

    return {
        "best_variant": results[0].content.text,
        "comparison": [
            {
                "content": r.content.text,
                "engagement_rate": r.engagement_rate,
                "expected_reach": r.total_reach,
